.row.grid-view
  - @spaces.each do |space|
    .space.col-xs-12.col-md-4
      .panel.panel-default
        .panel-heading
          .panel-title
            = link_to space.name, space

          .panel-actions.dropdown
            button.dropdown-toggle aria-expanded="false" data-toggle="dropdown" type="button"
              = icon 'chevron-down'

            ul.dropdown-menu.dropdown-right role="menu"
              li.icon-left
                = link_to icon('pencil') + 'Edit', edit_space_path(space), class: 'icon-left'
              li.icon-left
                = link_to icon('trash') + 'Delete', space, data: { confirm: 'Are you sure?' }, method: :delete, class: 'icon-left'

        .panel-body data-mh="panel-body"

        .panel-footer
