= render 'common/content_header', title: 'Projects'

- if @projects.any?

  - if params['view'] == 'list'
  	= render 'list_view'
  - else
    = render 'grid_view'

- else

  = render 'common/no_data'

= will_paginate @projects

= render 'sidebar'
